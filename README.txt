Projet TRON 
(Auteurs : Michel EDJOA, Daniel Rodriguez)

** Pour  compiler 
$> make

** Pour  executer, depuis le répertoire courant où se trouve le Makefile sinon l'application ne trouve pas les images. En mode texte :
$> bin/tron_txt
** En mode graphique :
$> bin/tron_gfx
